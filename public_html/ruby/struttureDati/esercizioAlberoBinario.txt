Implementa la classe Nodo coi seguenti metodi

initialize(valore)  #crea un nodo con all'interno un valore

valore              #restituisce il valore contenuto nel nodo

sx                  #restituisce il figlio sinistro del nodo

dx                  #restituisce il figlio destro del nodo

sx=(valore)         #crea il figlio sinistro con all'interno un valore

dx=(valore)         #crea il figlio destro con all'interno un valore

to_s                #restituisce il valore del nodo trasformato in stringa


Implementa la classe Albero (binario) coi i seguenti metodi:

to_s          #restituisce una stringa concatenando tutti i valori dei nodi

dimensione    #conta il numero di nodi

cerca         #determina se (true o false) un certo valore è presente

altezza       #determina l'altezza dell'albero
