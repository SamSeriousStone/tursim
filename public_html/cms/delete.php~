<?php
require_once 'paths.php';
require_once COMMON;
require_once FILE_MANAGER;
header("Content-Type: application/json");

$path=get_root().get('path');

$fm=new FileManager(get('host'));
if($fm->access(get('username'),get('password'))) echo json_encode($fm->delete($path));
else echo 'false';
?>
