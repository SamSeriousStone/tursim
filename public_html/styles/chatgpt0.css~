/* Sovrascrive il font di base */
html {
  font-family: 'Arial', sans-serif !important;
}

/* Cambia il colore di sfondo e i bordi delle finestre */
.win {
  background-color: #f0f8ff !important; /* Azzurro chiaro */
  border: 1px solid #4682b4 !important; /* Blu acciaio */
  outline: 1px solid #5f9ea0 !important; /* Verde cadetto */
}

/* Modifica il colore dei pulsanti nella dock */
.dock > .button {
  color: #4caf50 !important; /* Verde brillante */
  font-size: 2.5rem !important;
  opacity: 1 !important;
}

.dock > .button:hover {
  color: #2e7d32 !important; /* Verde scuro */
}

/* Modifica lo stile dei link */
a {
  color: #1e90ff !important; /* Blu brillante */
}

a:hover {
  color: #ff4500 !important; /* Arancione acceso */
  text-decoration: none !important;
}

/* Cambia lo stile delle celle delle tabelle */
td {
  border-color: #696969 !important; /* Grigio scuro */
  background-color: #f5f5f5 !important; /* Grigio chiaro */
}

/* Modifica i colori della barra laterale (dock) */
html[data-mobile=false] body > [data-nomen=dock] {
  background-color: #ffffff !important; /* Bianco */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3) !important; /* Ombra più leggera */
}

/* Cambia l'icona dei pulsanti nella dock */
body > [data-nomen=dock] > .control_button::before {
  content: "\f013" !important; /* Icona degli ingranaggi */
}

body > [data-nomen=dock] > .style_button::before {
  content: "\f53c" !important; /* Icona di pennello */
}

/* Personalizza l'aspetto degli header */
h2 {
  color: #4b0082 !important; /* Indaco */
  border-bottom: 2px solid #4b0082 !important; /* Linea decorativa */
  padding-bottom: 0.5rem;
}

/* Modifica il layout dei pulsanti nella barra laterale su desktop */
html[data-mobile=false] body > [data-nomen=dock] {
  transform: translate(0, 0) !important;
  opacity: 1 !important;
}

html[data-mobile=false] body > [data-nomen=dock]:hover {
  box-shadow: 0 6px 12px rgba(0, 0, 0, 0.5) !important; /* Ombra più marcata */
}
