/* Foglio di stile integrativo "caramelloso" */

:root {
  --primary-color: #ff9aa2;  /* Rosa pastello */
  --secondary-color: #ffdac1; /* Pesca pastello */
  --accent-color: #b5ead7;  /* Verde acqua pastello */
  --text-color: #6b4e71;  /* Viola tenue */
  --background-color: #fff1e6; /* Crema pastello */
  --border-color: #ffb7b2; /* Rosato */
  --hover-color: #ffb3ba; /* Rosa chiaro */
  --shadow-color: rgba(255, 182, 193, 0.5);
}

body {
  background-color: var(--background-color);
  color: var(--text-color);
}

a {
  color: var(--primary-color);
}

a:hover {
  color: var(--hover-color);
  text-decoration: underline wavy var(--primary-color);
}

h2 {
  color: var(--primary-color);
  font-family: "Comic Sans MS", cursive, sans-serif;
}

td {
  border-color: var(--border-color);
  background-color: var(--secondary-color);
}

.button {
  background-color: var(--primary-color);
  color: white;
  border-radius: 12px;
  font-weight: bold;
  transition: all 0.3s;
  box-shadow: 0 4px 6px var(--shadow-color);
}

.button:hover {
  background-color: var(--hover-color);
  box-shadow: 0 6px 8px var(--shadow-color);
}

.dock {
  background-color: var(--accent-color);
  border-radius: 16px;
  box-shadow: 0 4px 6px var(--shadow-color);
}

.toolbar {
  background-color: var(--secondary-color);
  border-bottom: 3px solid var(--border-color);
}

.win {
  border-radius: 16px;
  border: 3px solid var(--border-color);
  background-color: var(--background-color);
  box-shadow: 0 6px 8px var(--shadow-color);
}

.file_browser, .file_manager {
  background-color: var(--background-color);
  border: 2px solid var(--border-color);
  border-radius: 10px;
  box-shadow: 0 4px 6px var(--shadow-color);
}

